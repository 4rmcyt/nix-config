.DS_Store
result
result-topology
# nix-topology renders resolve real host/relay IPs from the private flake input —
# generate locally with `just topology`, never commit.
docs/topology.svg
docs/topology-network.svg
core
core.*
.vscode
.idea
.claude/*
!.claude/memory/
!.claude/CLAUDE.md
!.claude/settings.json
.flake-inputs
.mcp.json
.continue/
.git-crypt/
*_backup.tar.gz
gnupg_backup.tar.gz
.direnv
.direnv/flake-profile-a5d5b61aa8a61b7d9d765e1daf971a9a578f1cfa.rc

# Security: Ignore secrets directory (SOPS-encrypted files only!)
# Note: secrets/ is tracked but only encrypted files should be committed
secrets/*.backup
secrets/*.tmp
secrets/*.unencrypted
.decrypted.*
*.decrypted.*
*.sopsbak
tools/scripts/batch-log.txt
