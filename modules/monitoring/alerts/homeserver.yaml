groups:
  - name: Host Alerts
    rules:
      - alert: HighCPUUsage
        expr: avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100 < 20
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage on {{ $labels.instance }}"
          description: "CPU usage has been over 80% for the last 5 minutes."
      - alert: HighLoadAverage
        expr: node_load15 > count without(cpu, mode) (node_cpu_seconds_total{mode="idle"}) * 2
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "High load average on {{ $labels.instance }}"
          description: "15-minute load average is {{ $value | humanize }}, more than 2x CPU count for 10 minutes."
      - alert: HighMemoryUsage
        expr: node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes * 100 < 10
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Low available memory on {{ $labels.instance }}"
          description: "Less than 10% memory available ({{ $value | humanize }}% free)."
      - alert: LowDiskSpaceRoot
        expr: node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"} * 100 < 15
        for: 10m
        labels:
          severity: critical
        annotations:
          summary: "Low disk space on / ({{ $labels.instance }})"
          description: "Root filesystem has less than 15% free space."
      - alert: LowDiskSpaceData
        expr: node_filesystem_avail_bytes{mountpoint="/data"} / node_filesystem_size_bytes{mountpoint="/data"} * 100 < 10
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Low disk space on /data ({{ $labels.instance }})"
          description: "/data filesystem has less than 10% free space ({{ $value | humanize }}% remaining)."
      - alert: LowDiskSpaceBackup
        expr: node_filesystem_avail_bytes{mountpoint="/backup"} / node_filesystem_size_bytes{mountpoint="/backup"} * 100 < 10
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Low disk space on /backup ({{ $labels.instance }})"
          description: "/backup filesystem has less than 10% free space ({{ $value | humanize }}% remaining)."
      - alert: ZfsPoolUnhealthy
        expr: node_zfs_zpool_state{state!="online"} == 1
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "ZFS pool {{ $labels.zpool }} is {{ $labels.state }} on {{ $labels.instance }}"
          description: "ZFS pool '{{ $labels.zpool }}' is not online. Immediate investigation required."
      - alert: ServiceDown
        expr: up{job!~"matebook-node|desktop-node|router-node|router-unbound|router-kea"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Service {{ $labels.job }} is down on {{ $labels.instance }}"
          description: "Prometheus job '{{ $labels.job }}' has been unreachable for more than 2 minutes."
  - name: UPS Alerts
    rules:
      - alert: UpsOnBattery
        expr: network_ups_tools_ups_status{flag="OB"} == 1
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "UPS is on battery"
          description: "UPS lost line power (OB flag set). Running on battery."
      - alert: UpsLowBattery
        expr: network_ups_tools_battery_charge < 30
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "UPS battery low: {{ $value }}%"
          description: "UPS battery charge is critically low. Graceful shutdown may be imminent."
      - alert: UpsHighLoad
        expr: network_ups_tools_ups_load > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "UPS load high: {{ $value }}%"
          description: "UPS load has been above 80% for 5 minutes."
      - alert: UpsForcedShutdown
        expr: network_ups_tools_ups_status{flag="FSD"} == 1
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "UPS forced shutdown in progress"
          description: "UPS FSD (Forced Shutdown) flag is set — imminent power loss."
  - name: job-kombayn Alerts
    rules:
      - alert: JobKombaynRunFailed
        expr: job_kombayn_last_run_success == 0
        for: 0m
        labels:
          severity: warning
        annotations:
          summary: "job-kombayn scan failed for {{ $labels.profile }}"
          description: "The last scan run for profile '{{ $labels.profile }}' exited with an error. Check the systemd journal for job-kombayn.service."
      - alert: JobKombaynStale
        # job-kombayn.timer runs every 3h (homeserver onCalendar "*-*-* 0/3:00:00")
        # + up to 3min RandomizedDelaySec + up to 20min TimeoutStartSec, so the
        # gap between two healthy successful runs can legitimately reach ~3h25m.
        expr: time() - job_kombayn_last_run_timestamp_seconds > 13800
        for: 0m
        labels:
          severity: warning
        annotations:
          summary: "job-kombayn hasn't run for {{ $labels.profile }} in over 3h50m"
          description: "No successful/attempted scan recorded for profile '{{ $labels.profile }}' in the last 3h50m (timer fires every 3h) — job-kombayn.timer may be stopped or the textfile-collector ACL may be broken."
      - alert: JobKombaynRunErrors
        expr: job_kombayn_errors > 0
        for: 0m
        labels:
          severity: warning
        annotations:
          summary: "job-kombayn had {{ $value }} error(s) on last run for {{ $labels.profile }}"
          description: "The last scan for profile '{{ $labels.profile }}' logged {{ $value }} error(s) (e.g. Telegram delivery failures) even though the run itself succeeded."
